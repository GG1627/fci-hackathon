# Optional. By default, the dashboard uses port 8000 on the same hostname.
# Set this when the Pi API is running on a different device or hostname.
NEXT_PUBLIC_FRIDGEGUARD_API_URL=http://gaels-pi-5.local:8000
